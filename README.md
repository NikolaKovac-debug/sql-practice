# SQL Practice Web

This folder is the static GitHub Pages build of the SQL practice app.

Deploy all files in this directory to GitHub Pages, Vercel, Netlify, or any static hosting service.

The app runs SQLite in the browser through local `sql.js` assets under `vendor/sqljs/`. It does not send learner SQL to a backend service.

